//Reverse the string

ANS:
#include <bits/stdc++.h> 

string reverseString(string str)
{
	// Write your code here.
	int i=0,j=str.length()-1;
	while(i<=j)
	{
		swap(str[i],str[j]);
		i++;
		j--;
	}
return str;
}
